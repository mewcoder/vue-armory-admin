import { ref } from 'vue';

// export default function useRequest(api, defaultValue) {
//   const res = ref(defaultValue);
//   api()
//     .then((json: any) => {
//       res.value = json.data;
//     })
//     .finally(() => {});
//   return { res };
// }
